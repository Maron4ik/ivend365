# Static markup for iVend365 Customer App

To run project locally run `npm install` and then `npm run start` to look on webpack-dev-server or `npm run build` to get compiled files in folder with name **compiled**.
